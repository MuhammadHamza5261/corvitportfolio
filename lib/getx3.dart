import 'package:flutter/material.dart';
class getx3 extends StatelessWidget {
  const getx3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Container(
          child: Text('screen 3'),
        ),
      ),
    );
  }
}

